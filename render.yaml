services:
  - type: web
    name: bhookle-ai-usecases
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      # DATABASE_URL is provided by the linked Render Postgres instance.
